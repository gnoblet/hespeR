% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HesperEnhanced.R
\name{HesperEnhanced}
\alias{HesperEnhanced}
\title{S7 class for hespeR data.frames (enhanced)}
\usage{
HesperEnhanced(
  df = (function (.data = list(), row.names = NULL) 
 {
     if (is.null(row.names)) {
  
          list2DF(.data)
     }
     else {
         out <- list2DF(.data,
    length(row.names))
         attr(out, "row.names") <- row.names
         out
     }

    })(),
  sl_list = list(),
  extra_cols = character(0)
)
}
\arguments{
\item{df}{(\verb{data.frame[,1+]}) A data frame containing HESPER items, HESPER response options, and optionally further columns.}

\item{sl_list}{(\code{list[SL]}) Optional list of SL (skip logic) S7 objects, each describing a skip logic rule for the data. See details for input validation.}

\item{extra_cols}{(\verb{character\{0+\}}) Optional character vector of additional column names in the data frame (e.g., population group, site, etc.).}
}
\description{
The \code{HesperEnhanced} class extends \code{HesperDefault} to support enhanced HESPER data frames.
It allows for additional columns beyond the standard HESPER variables, and supports skip logic rules via a list of S7 \code{SL} objects.
}
\details{
\itemize{
\item Inherits all properties and validation from \code{HesperDefault}.
\item Allows for extra columns in the data frame, such as population group, site, or other metadata.
\item Optionally accepts a list of S7 \code{SL} objects, each representing a skip logic rule.
\item The validator checks:
\itemize{
\item All extra columns (if specified or inferred) exist in the data frame.
\item For \code{sl_list}:
\itemize{
\item Must be a list of S7 \code{SL} objects.
\item For each SL object:
\itemize{
\item \code{subset_var} exists in the extra columns (not in HESPER variables).
\item The type of \code{subset_var} in the data frame matches the type expected by the SL object (using \code{typeof} and factor checks).
}
\item No duplicate \code{subset_var} or \code{hesper_var} within the SL list.
}
}
\item Additional columns are simply part of the data frame; no further validation is performed on their values.
}
}
