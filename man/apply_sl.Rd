% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HesperListEnhanced.apply_hesper_list_sl.R
\name{apply_sl}
\alias{apply_sl}
\title{Apply SL cleaning to both hesper_list and hesper_priorities}
\usage{
apply_sl(self, cascading = "missing")
}
\arguments{
\item{self}{(\code{HesperListEnhanced}) A HesperListEnhanced object}

\item{cascading}{(\code{character[1]}) The cascading behavior for missing values. Options are "missing" or "upcycle". See \code{\link[=apply_priority_list_sl]{apply_priority_list_sl()}}.}
}
\value{
(\code{HesperListEnhanced}) The cleaned HesperListEnhanced object after applying skip logic to both components
}
\description{
Apply SL cleaning to both hesper_list and hesper_priorities
}
\details{
This method applies skip logic rules to both the hesper_list and hesper_priorities
components of a HesperListEnhanced object. It sequentially calls:
\enumerate{
\item \code{\link[=apply_hesper_list_sl]{apply_hesper_list_sl()}} to clean the HESPER response data
\item \code{\link[=apply_priority_list_sl]{apply_priority_list_sl()}} to clean the priority rankings
}

This ensures consistency between the HESPER responses and priority data -
if a respondent shouldn't answer a particular HESPER item due to skip logic,
that item is also removed from their priority rankings.
}
