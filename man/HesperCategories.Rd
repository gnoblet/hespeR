% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HesperCategories.R
\name{HesperCategories}
\alias{HesperCategories}
\title{S7 class for HESPER categories list}
\usage{
HesperCategories(category_list = list())
}
\arguments{
\item{category_list}{(\code{list[HesperCategory]}) A list of \code{HesperCategory} items, each representing a thematic category and its associated HESPER variables.}
}
\value{
(\code{S7_object}) A S7 object of class \code{HesperCategories}, representing a collection of HESPER categories.
}
\description{
S7 class for HESPER categories list
}
\details{
The \code{HesperCategories} class is designed to encapsulate a collection of thematic categories for HESPER items.
Each category in the list is an instance of the \code{HesperCategory} class.

The class validates that:
\itemize{
\item Each item in the list is a valid \code{HesperCategory}
\item Category names (cat) are unique across all categories
\item HESPER variables (vars) are not duplicated across categories (each variable belongs to only one category)
\item The list is not empty
}
}
\examples{
# Create individual categories
health_cat <- HesperCategory(
  cat = "health",
  vars = c("hesper_health", "hesper_health_care_male")
)
fsl_cat <- HesperCategory(
  cat = "fsl",
  vars = c("hesper_food")
)
wash_cat <- HesperCategory(
  cat = "wash",
  vars = c("hesper_drinking_water", "hesper_toilet")
)

# Create categories collection
categories <- HesperCategories(
  category_list = list(health_cat, fsl_cat, wash_cat)
)

# Get category names
names(categories)

# Get variables for a specific category
get_category_vars(categories, "health")

}
